sampler:
  name: metropolis
  burn_in: 0
  steps_per_iteration: 1
  proposal_width: 0.02
  adapt_frequency: 10
  one_electron: false
  importance_sampling: false

notes:
  status: config_build_ready
  purpose: minimal DeepSolid sampler settings for import/config smoke tests
